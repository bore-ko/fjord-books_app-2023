<p style="color: green"><%= notice %></p>

<div class="show-items">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('button.edit_book'), edit_book_path(@book) %> |
  <%= link_to t('button.back_books'), books_path %>

  <%= button_to t('button.destroy_book'), @book, method: :delete %>
</nav>
